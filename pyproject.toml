[project]
name = "tasktracker"
requires-python = ">=3.12,<3.13"

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "config.settings"
python_files = ["test_*.py", "tests.py"]
testpaths = ["apps"]

[tool.ruff]
# Placeholder for Phase 2 lint config; version pin lives in requires-python above.
